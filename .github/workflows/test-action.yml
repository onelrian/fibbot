name: Test Rust Hello World Action

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Rust Hello World Action
        uses: ./

      - name: Verify Output
        run: echo "Check GitHub Actions logs for 'Hello, world!' output"
