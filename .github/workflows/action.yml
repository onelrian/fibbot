name: Test Rust Fibonacci Action

on: [push, pull_request] 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Rust Fibonacci Action
        uses: ./
        with:
          number: "10" 
